import React from 'react';

import { Ionicons } from '@expo/vector-icons';

import { theme } from '../global/styles/global';

export const config = {
    avatar: <Ionicons name="person-outline"size={30} style={{color: theme.colors.dark_grey}}/>
}