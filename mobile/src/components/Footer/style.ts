import {
    StyleSheet
} from 'react-native';

export const style = StyleSheet.create({
    container: {
        width: '100%',
        height: 120,
        position: 'absolute',
        zIndex: 1,
        bottom: 0
    },
});